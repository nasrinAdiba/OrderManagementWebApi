
@{
    ViewBag.Title = "Order Management";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<script src="~/lib/jquery/dist/jquery.min.js"></script>

<h1>Order Management Application</h1>
<form>
	<label for="itemName">Item Name</label>
	<input type="text" id="itemName" name="itemName" required>

	<label for="unitPrice">Unit Price</label>
	<input type="number" id="unitPrice" name="unitPrice" required>

	<label for="quantity">Quantity</label>
	<input type="number" id="quantity" name="quantity" required>

	<label for="discount">Discount</label>
	<input type="number" id="discount" name="discount" required>

	<label for="orderInvoiceNo">Order Invoice No</label>
	<input type="text" id="orderInvoiceNo" name="orderInvoiceNo" required>

	<label for="orderDateTime">Order Date & Time</label>
	<input type="datetime-local" id="orderDateTime" name="orderDateTime" required>

	<label for="totalPrice">Total Price</label>
	<input type="number" id="totalPrice" name="totalPrice" required>

	<label for="customerName">Customer Name</label>
	<input type="text" id="customerName" name="customerName" required>

	<label for="customerAddress">Customer Address</label>
	<input type="text" id="customerAddress" name="customerAddress" required>

	<label for="shippingAddress">Shipping Address</label>
	<input type="text" id="shippingAddress" name="shippingAddress" required>

	<label for="shippingDate">Shipping Date</label>
	<input type="date" id="shippingDate" name="shippingDate" required>

	<input type="submit" value="Submit" style="margin-top: 50px;">
</form>

<script>
    function GetAllOrderInfoes() {
        $.ajax({
            url: '/api/OrderInfoes',
            type: 'GET',
            success: function (data) {
                // Update the item list on the page
                // Use JavaScript DOM manipulation or a JavaScript framework like Vue.js or Angular to update the UI
            },
            error: function (error) {
                console.log(error);
            }
        });
    }

    // Handle form submission for creating or editing an item
    function AddUpdateOrderinfo(orderinfo) {
        $.ajax({
            url: '/api/OrderInfoes',
            type: 'POST',
            data: item,
            success: function () {
                // Redirect to the item list page or update the UI accordingly
            },
            error: function (error) {
                console.log(error);
            }
        });
    }

    // Handle deletion of an item
    function Delete(id) {
        $.ajax({
            url: '/api/OrderInfoes/' + id,
            type: 'DELETE',
            success: function () {
                // Redirect to the item list page or update the UI accordingly
            },
            error: function (error) {
                console.log(error);
            }
        });
    }

    // Similar functions can be implemented for orders and customers

    // Call the loadItems function when the page is ready
    $(document).ready(function () {
        loadItems();
    });
	</script>